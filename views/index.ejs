<!DOCTYPE html>
<html>
<head>
  <title><%= title %></title>
</head>
<body>
  <h1><%= title %></h1>
  <ul>
    <% routes.forEach(route => { %>
      <li><strong><%= route.method %></strong> — <code><%= route.path %></code>: <%= route.description %></li>
    <% }) %>
  </ul>
</body>
</html>
